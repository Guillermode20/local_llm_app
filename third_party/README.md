# Third-Party Dependencies

- `llama.cpp` is pinned as a git submodule at `third_party/llama.cpp` on tag `b9009`.
- The native Android build expects `third_party/OpenCL-Headers` and `third_party/OpenCL-ICD-Loader` checkouts for the OpenCL stub library when that backend is enabled.